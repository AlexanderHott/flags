POSTGRES_USER=flags
POSTGRES_PASSWORD=change-me
POSTGRES_DB=flags

# Used only by the local Docker Compose override.
POSTGRES_PORT=5432

# Containers should use the Compose service name, not localhost.
DATABASE_URL=postgresql://flags:change-me@postgres:5432/flags
